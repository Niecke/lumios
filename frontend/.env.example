# Google OAuth client ID for the lumios-frontend OAuth client (from Google Cloud Console)
# Must match GOOGLE_FRONTEND_CLIENT_ID in the backend .env
VITE_GOOGLE_CLIENT_ID=

# Base URL of the landing page — used for AGB/Datenschutz links in the registration form
# Leave empty if the landing page is on the same origin
VITE_LANDING_URL=
